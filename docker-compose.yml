services:
  database:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: motor-scoring-database
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: "Your_password123!"
    ports:
      - "1433:1433"
    volumes:
      - motor_scoring_sql:/var/opt/mssql
    healthcheck:
      test: ["CMD-SHELL", "/opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P 'Your_password123!' -C -Q 'SELECT 1' || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 20
    restart: unless-stopped

  motor-scoring-identity:
    build:
      context: ./MotorScoring.Identity
      dockerfile: Dockerfile
    container_name: motor-scoring-identity
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_URLS: "http://+:8082"
      ConnectionStrings__IdentityDb: "Server=database,1433;Database=MotorScoringIdentity;User Id=sa;Password=Your_password123!;TrustServerCertificate=True;Encrypt=False"
      Jwt__Issuer: "MotorScoring.Identity"
      Jwt__Audience: "MotorScoring.Api"
      Jwt__SigningKey: "DEV-ONLY-MotorScoring-Signing-Key-Change-Me-2026-At-Least-32-Bytes"
      WebClient__BaseUrl: "http://localhost:8081"
      Seed__AdminEmail: "admin@motorscoring.local"
      Seed__AdminPassword: "Admin1234*"
      Authentication__Google__ClientId: ""
      Authentication__Google__ClientSecret: ""
      Authentication__GitHub__ClientId: ""
      Authentication__GitHub__ClientSecret: ""
    ports:
      - "8082:8082"
    depends_on:
      database:
        condition: service_healthy
    restart: unless-stopped

  motor-scoring-api:
    build:
      context: ./MotorScoring.Hexagonal.Net8
      dockerfile: src/MotorScoring.Api/Dockerfile
    container_name: motor-scoring-api
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_URLS: "http://+:8080"
      ConnectionStrings__MotorScoringDb: "Server=database,1433;Database=MotorScoring;User Id=sa;Password=Your_password123!;TrustServerCertificate=True;Encrypt=False"
      Jwt__Issuer: "MotorScoring.Identity"
      Jwt__Audience: "MotorScoring.Api"
      Jwt__SigningKey: "DEV-ONLY-MotorScoring-Signing-Key-Change-Me-2026-At-Least-32-Bytes"
    ports:
      - "8080:8080"
    depends_on:
      database:
        condition: service_healthy
    restart: unless-stopped

  motor-scoring-web:
    build:
      context: ./MotorScoring.Web
      dockerfile: Dockerfile
    container_name: motor-scoring-web
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_URLS: "http://+:8081"
      MotorScoringApi__BaseUrl: "http://motor-scoring-api:8080"
      Identity__BaseUrl: "http://motor-scoring-identity:8082"
      Identity__PublicBaseUrl: "http://localhost:8082"
    ports:
      - "8081:8081"
    depends_on:
      - motor-scoring-api
      - motor-scoring-identity
    restart: unless-stopped

volumes:
  motor_scoring_sql:
